import React from "react";
import { Provider } from "react-redux"; // wrap this around everything for redux

const Root = () => {
  return (
    <div>
      <p>HomePage</p>
    </div>
  );
};

export default Root;
